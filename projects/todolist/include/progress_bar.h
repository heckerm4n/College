#ifndef PROGRESS_BAR_H
#define PROGRESS_BAR_H

void init_progress_bar();          // Initialize the progress bar settings
void render_progress_bar(int progress);  // Render the progress bar based on the progress percentage

#endif

